[package]
name = "zenith-rendergraph"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow.workspace = true
bytemuck.workspace = true
profiling.workspace = true
smallvec = "1.15"
zenith-rhi = { path = "../zenith-rhi" }

[dev-dependencies]
zenith-core = { path = "../zenith-core" }
